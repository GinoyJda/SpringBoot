<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.neusoft.mybatis">
    <!-- 查询 -->
    <select id="selectTest" resultType="com.neusoft.beans.User">
		select
			host ,
			user
		from
		 	user
	</select>
</mapper>